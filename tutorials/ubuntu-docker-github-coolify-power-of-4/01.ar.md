---
SPDX-License-Identifier: MIT
path: "/tutorials/ubuntu-docker-github-coolify-power-of-4"
slug: "ubuntu-docker-github-coolify-power-of-4"
date: "2025-12-19"
title: "قوة الأربعة: Ubuntu و Docker و GitHub و Coolify"
short_description: "تعلم كيفية الاستفادة من القوة المشتركة لـ Ubuntu و Docker و GitHub و Coolify لنشر التطبيقات بكفاءة."
tags: ["Ubuntu", "Docker", "GitHub", "Coolify"]
author: "Your Name"
author_link: "https://github.com/....."
author_img: "https://avatars3.githubusercontent.com/u/....."
author_description: "Manipulating arrays of characters in modern text editors that need more RAM than we used to fly to the moon. But it's super awesome..."
language: "ar"
available_languages: ["en", "ar"]
header_img: "header-x"
cta: "cloud"
---

![Power of 4 Header](./architecture.png)


## المقدمة

يشرح هذا الدليل كيفية دمج «قوة الأربعة» — ‎Ubuntu + Docker + GitHub + Coolify — لنشر تطبيق ويب مُحcontainer على خادم Hetzner مع بناء تلقائي وHTTPS.

في النهاية ستحصل على:
* خادم Ubuntu مؤمَّن مع Docker وCompose.
* تطبيق نموذجي في GitHub مع Dockerfile.
* تثبيت Coolify يسحب من المستودع، يبني، ينشر، ويُفعّل الشهادات تلقائياً.

## المتطلبات المسبقة

* خادم Hetzner Cloud (Ubuntu 22.04/24.04) مع IP عام وإمكانية SSH.
* دومين أو نطاق فرعي موجّه إلى IP الخادم (تسجيل A/AAAA).
* حساب GitHub ورمز وصول شخصي PAT بصلاحيات `repo` و`admin:repo_hook` لـ Coolify.
* (مستحسن) مفتاح SSH على الخادم ومضاف إلى GitHub.

## المعمارية: قوة الأربعة

![معمارية قوة الأربعة](./architecture.png)


1. **Ubuntu**: نظام أساسي مستقر.
2. **Docker**: تشغيل حاويات ببناء قابل للتكرار.
3. **GitHub**: مصدر الكود ومحفّز البناء.
4. **Coolify**: طبقة PaaS تسحب، تبني، تشغّل، وتدير SSL.

## الخطوة 1 — تجهيز الخادم

```bash
sudo apt update && sudo apt upgrade -y
sudo timedatectl set-timezone UTC
sudo apt install -y ca-certificates curl gnupg git ufw
sudo ufw allow OpenSSH && sudo ufw enable
```

## الخطوة 2 — تثبيت Docker وCompose

```bash
curl -fsSL https://get.docker.com | sudo sh
sudo usermod -aG docker $USER
newgrp docker
docker --version
docker compose version
```

## الخطوة 3 — إنشاء تطبيق تجريبي ودفعه إلى GitHub

إذا كان لديك تطبيق جاهز تأكد من وجود `Dockerfile`. وإلا أنشئ مثال Node بسيط:

```bash
mkdir power-of-4-app && cd power-of-4-app
cat > server.js <<'EOF'
const http = require('http');
const port = process.env.PORT || 3000;
const msg = `Hello from the Power of 4: ${new Date().toISOString()}\n`;
http.createServer((_, res) => { res.writeHead(200); res.end(msg); }).listen(port);
console.log(`Listening on ${port}`);
EOF

cat > package.json <<'EOF'
{ "name": "power-of-4", "version": "1.0.0", "main": "server.js", "scripts": { "start": "node server.js" } }
EOF

cat > Dockerfile <<'EOF'
FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json* ./ 
RUN npm install --production || npm install --omit=dev
COPY . .
ENV PORT=3000
EXPOSE 3000
CMD ["npm", "start"]
EOF
```

تهيئة ودفع التغييرات:

```bash
git init
git add .
git commit -m "Initial Power of 4 app"
git branch -M main
git remote add origin git@github.com:<your-username>/<your-repo>.git
git push -u origin main
```

## الخطوة 4 — تثبيت Coolify على Ubuntu

```bash
curl -fsSL https://cdn.coollabs.io/coolify/install.sh | bash
```

بعد التثبيت افتح `http://<server-ip>:3000`، أنشئ حساب المشرف، وأكمل الإعداد.

## الخطوة 5 — إضافة الدومين إلى الخادم

1. أنشئ سجل A (وAAAA لـ IPv6) يوجّه الدومين/الفرعي إلى IP الخادم.
2. في Coolify: **Infrastructure → Servers → Domains** لإضافة الدومين حتى يتم إصدار TLS لاحقاً.

## الخطوة 6 — ربط GitHub مع Coolify

1. في Coolify: **Settings → Git Providers → Add GitHub**.
2. الصق رمز PAT بصلاحيات `repo` و`admin:repo_hook`.
3. احفظ وانتظر التحقق.

## الخطوة 7 — نشر التطبيق من GitHub

1. في Coolify، أنشئ/اختر مشروعاً → **New Resource → Application**.
2. اختر **Git** → المستودع والفرع `main`.
3. إعدادات البناء:
   * **Type**: Dockerfile
   * **Dockerfile path**: `Dockerfile`
   * **Port**: `3000`
4. اختر الخادم كوجهة نشر ثم اضغط **Deploy** ليتم السحب والبناء والتشغيل.

## الخطوة 8 — تفعيل HTTPS

1. في إعدادات **Domain** للتطبيق أضف الدومين/الفرعي.
2. فعّل **Enable SSL** لطلب شهادة Let’s Encrypt.
3. أعد النشر إذا طُلِب، ثم زر `https://your-domain`.

## الخطوة 9 — التحقق والتحديث

* عرض السجلات: **Application → Logs**.
* فحص الحالة: `curl -I https://your-domain`.
* للتحديثات: ادفع إلى `main` ثم أعد النشر (أو فعّل النشر التلقائي عند الدفع).

## الصيانة واستكشاف الأخطاء

* إعادة تشغيل: **Application → Settings → Restart**.
* فشل البناء: تأكد من مسار Dockerfile ورقم المنفذ.
* مشكلة SSL: تحقق من انتشار DNS وفتح المنافذ 80/443.

## الخاتمة

جمعت بين Ubuntu وDocker وGitHub وCoolify لنشر تطبيق حاويات مع بناء متكرر، إعادة نشر بدون توقف، وHTTPS تلقائي. وسّع ذلك بإضافة قواعد بيانات في Coolify، بيئات تجريبية، أو GitHub Actions لاختبارات CI.

**الخطوات التالية:**

* أضف قاعدة بيانات (PostgreSQL/Redis) عبر Coolify واربط متغيرات البيئة.
* احمِ فرع main بقواعد الحماية في GitHub والفحوصات الإلزامية.
* استكشف النسخ الاحتياطي أو التوسعة للتطبيقات ذات الحالة.

## تخصيصات متقدمة 🎨

### المظهر & تعديل الواجهة
- خصص سمة واجهة Coolify عبر تعديل ملف `coolify.yml`. استخدم متغيرات CSS لتطابق ألوان علامتك التجارية.
- فعّل الوضع الداكن لمظهر أنيق: أضف `--theme: dark` ضمن إعدادات **المظهر**.

### المراقبة & السجلات 📈
- دمج Prometheus و Grafana مع Coolify لمراقبة مقاييس الحاويات.
- فعّل تدفق السجلات في **Application → Logs** وأرسلها إلى مجموعة ELK مركزية.

## نصائح استكشاف الأخطاء 🛠️
- **Docker daemon لا يستجيب**: نفّذ `sudo systemctl restart docker` وتحقق بـ `docker ps`.
- **فشل Webhook في GitHub**: تأكد من مطابقة سر الـ webhook مع ما تم تكوينه في Coolify.
- **أخطاء TLS**: تحقق من فتح المنافذ 80/443 وانتشار DNS.

## الأسئلة الشائعة 🤔
**س:** هل يمكن نشر تطبيقات متعددة على نفس الخادم؟
**ج:** نعم، يدعم Coolify مشاريع متعددة؛ فقط خصص نطاقات مختلفة لكل تطبيق.

**س:** كيف أستعيد نسخة سابقة من النشر؟
**ج:** استخدم تبويب **Deployments** في Coolify وانقر **Rollback** على نسخة سابقة ناجحة.

## إضافة CI/CD 🚀
أضف سير عمل GitHub Actions لتشغيل الاختبارات قبل بناء Coolify:
```yaml
name: CI
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install dependencies
        run: npm ci
      - name: Run tests
        run: npm test
```
هذا يضمن أن الكود الذي يصل إلى Coolify قد اجتاز جميع الاختبارات.


##### License: MIT

<!--

Contributor's Certificate of Origin

By making a contribution to this project, I certify that:

(a) The contribution was created in whole or in part by me and I have
    the right to submit it under the license indicated in the file; or

(b) The contribution is based upon previous work that, to the best of my
    knowledge, is covered under an appropriate license and I have the
    right under that license to submit that work with modifications,
    whether created in whole or in part by me, under the same license
    (unless I am permitted to submit under a different license), as
    indicated in the file; or

(c) The contribution was provided directly to me by some other person
    who certified (a), (b) or (c) and I have not modified it.

(d) I understand and agree that this project and the contribution are
    public and that a record of the contribution (including all personal
    information I submit with it, including my sign-off) is maintained
    indefinitely and may be redistributed consistent with this project
    or the license(s) involved.

Signed-off-by: [submitter's name and email address here]

-->
