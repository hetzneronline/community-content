---
SPDX-License-Identifier: MIT
path: "/tutorials/ubuntu-docker-github-coolify-power-of-4"
slug: "ubuntu-docker-github-coolify-power-of-4"
date: "2025-12-19"
title: "🚀 قوة الأربعة: Ubuntu و Docker و GitHub و Coolify"
short_description: "أتقن فن النشر الحديث من خلال الجمع بين Ubuntu و Docker و GitHub و Coolify في سير عمل سلس وآلي مع نشر بدون توقف وHTTPS تلقائي."
tags: ["Ubuntu", "Docker", "GitHub", "Coolify", "DevOps", "CI/CD", "Containers", "Self-Hosted", "PaaS"]
author: "Your Name"
author_link: "https://github.com/....."
author_img: "https://avatars3.githubusercontent.com/u/....."
author_description: "مهتم بـ DevOps وشغوف بالحوسبة السحابية والأتمتة وبناء البنية التحتية القابلة للتوسع."
language: "ar"
available_languages: ["en", "ar"]
header_img: "header-x"
cta: "cloud"
---

![معمارية قوة الأربعة](./architecture.png)

---

## 🌟 المقدمة

مرحباً بك في الدليل الشامل لنشر التطبيقات الحديثة! في هذا الدليل المفصّل، ستتعلم كيفية تسخير **"قوة الأربعة"** — مزيج قوي من التقنيات سيغير طريقة نشر وإدارة تطبيقاتك.

> 💡 **لماذا "قوة الأربعة"؟** كل تقنية تجلب قوة خارقة فريدة لسير عمل النشر:
> - **Ubuntu** يوفر استقراراً صلباً
> - **Docker** يضمن التناسق عبر البيئات
> - **GitHub** يمكّن التعاون والتحكم بالإصدارات
> - **Coolify** يؤتمت كل شيء بواجهة جميلة

### ماذا ستحقق

بنهاية هذا الدليل، سيكون لديك:

| المكون | ما ستحصل عليه |
|--------|---------------|
| 🖥️ **الخادم** | مضيف Ubuntu محصّن مع Docker و Compose |
| 📦 **التطبيق** | تطبيق في حاوية مخزّن على GitHub |
| 🚀 **النشر** | بناء آلي يُفعّل عند الدفع بـ git |
| 🔒 **الأمان** | شهادات SSL/TLS مجانية عبر Let's Encrypt |
| 📊 **المراقبة** | سجلات فورية وتاريخ النشر |

---

## 📋 المتطلبات المسبقة

قبل البدء، تأكد من توفر:

### المطلوب
- [ ] **خادم Hetzner Cloud** (Ubuntu 22.04 أو 24.04 LTS)
  - الحد الأدنى: CX11 (1 vCPU، 2GB RAM)
  - الموصى به: CX21 (2 vCPU، 4GB RAM) للإنتاج
- [ ] **عنوان IP عام** مع وصول SSH
- [ ] **نطاق أو نطاق فرعي** مع سجل DNS من نوع A يشير لخادمك
- [ ] **حساب GitHub** مع رمز وصول شخصي (PAT)

### الموصى به
- [ ] زوج مفاتيح SSH مُعَدّ على الخادم
- [ ] معرفة أساسية بسطر أوامر Linux
- [ ] فهم مفاهيم الحاويات

> ⚠️ **مهم**: رمز PAT الخاص بـ GitHub يحتاج هذه الصلاحيات:
> - `repo` — تحكم كامل بالمستودعات الخاصة
> - `admin:repo_hook` — تحكم كامل بـ webhooks المستودعات

---

## 🏗️ نظرة عامة على المعمارية: قوة الأربعة

![معمارية قوة الأربعة](./architecture.png)

### كيف تعمل المكونات معاً

```
┌─────────────────────────────────────────────────────────────────┐
│                      سير عمل المطور                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   [الكود] ──push──▶ [GitHub] ──webhook──▶ [Coolify]              │
│                         │                    │                   │
│                         │                    ▼                   │
│                         │            ┌──────────────┐            │
│                         │            │   البناء     │            │
│                         │            │   والنشر     │            │
│                         │            └──────┬───────┘            │
│                         │                   │                    │
│                         ▼                   ▼                    │
│                   ┌─────────────────────────────────┐            │
│                   │         خادم UBUNTU             │            │
│                   │  ┌─────────────────────────┐    │            │
│                   │  │     محرك DOCKER         │    │            │
│                   │  │  ┌───────┐ ┌───────┐   │    │            │
│                   │  │  │تطبيق 1│ │تطبيق 2│   │    │            │
│                   │  │  └───────┘ └───────┘   │    │            │
│                   │  └─────────────────────────┘    │            │
│                   └─────────────────────────────────┘            │
│                                    │                             │
│                                    ▼                             │
│                          [HTTPS: your-domain.com]                │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### التعمق في المكونات

| # | المكون | الدور | القوة الخارقة |
|---|--------|-------|---------------|
| 1 | **Ubuntu** | نظام التشغيل | استقرار صلب، إدارة حزم ممتازة، دعم مجتمع واسع |
| 2 | **Docker** | تشغيل الحاويات | بيئات متسقة، عزل، توسيع سهل، بناء قابل للتكرار |
| 3 | **GitHub** | التحكم بالمصدر | تاريخ الإصدارات، التعاون، webhooks، تكامل Actions |
| 4 | **Coolify** | منصة PaaS | نشر بنقرة واحدة، SSL تلقائي، إدارة قواعد البيانات، تحديثات بدون توقف |

---

## 🔧 الخطوة 1 — تجهيز الخادم

أولاً، دعنا نؤمّن ونحدّث خادم Ubuntu.

### 1.1 الاتصال بالخادم

```bash
ssh root@<عنوان-ip-الخادم>
```

### 1.2 تحديث حزم النظام

```bash
# تحديث قوائم الحزم وترقية جميع الحزم
sudo apt update && sudo apt upgrade -y

# ضبط المنطقة الزمنية على UTC للسجلات المتسقة
sudo timedatectl set-timezone UTC

# تثبيت الأدوات الأساسية
sudo apt install -y \
    ca-certificates \
    curl \
    gnupg \
    git \
    ufw \
    htop \
    vim
```

### 1.3 تكوين جدار الحماية

```bash
# السماح باتصالات SSH (لا تحجب نفسك!)
sudo ufw allow OpenSSH

# السماح بـ HTTP و HTTPS لحركة الويب
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# تفعيل جدار الحماية
sudo ufw enable

# التحقق من الحالة
sudo ufw status verbose
```

> 🔐 **نصيحة احترافية**: فكّر في تغيير منفذ SSH الافتراضي وتعطيل مصادقة كلمة المرور لأمان معزز:
> ```bash
> # تحرير إعدادات SSH
> sudo vim /etc/ssh/sshd_config
> # اضبط: PasswordAuthentication no
> # اضبط: Port 2222 (أو منفذ آخر)
> sudo systemctl restart sshd
> ```

---

## 🐳 الخطوة 2 — تثبيت Docker و Docker Compose

Docker هو محرك الحاويات الذي يجعل النشر القابل للتكرار ممكناً.

### 2.1 تثبيت Docker باستخدام السكربت الرسمي

```bash
# تحميل وتشغيل سكربت تثبيت Docker الرسمي
curl -fsSL https://get.docker.com | sudo sh

# إضافة المستخدم لمجموعة docker (تجنب الحاجة لـ sudo مع أوامر docker)
sudo usermod -aG docker $USER

# تطبيق تغييرات المجموعة بدون تسجيل خروج
newgrp docker
```

### 2.2 التحقق من التثبيت

```bash
# التحقق من إصدار Docker
docker --version
# المتوقع: Docker version 24.x.x, build xxxxxxx

# التحقق من إصدار Docker Compose
docker compose version
# المتوقع: Docker Compose version v2.x.x

# تشغيل حاوية اختبار
docker run hello-world
```

### 2.3 تكوين Docker للإنتاج

```bash
# إنشاء تكوين daemon Docker
sudo tee /etc/docker/daemon.json <<EOF
{
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  },
  "live-restore": true,
  "default-ulimits": {
    "nofile": {
      "Name": "nofile",
      "Hard": 64000,
      "Soft": 64000
    }
  }
}
EOF

# إعادة تشغيل Docker لتطبيق التغييرات
sudo systemctl restart docker
```

> 📝 **ماذا يفعل هذا**:
> - يحدد أحجام ملفات السجل لمنع مشاكل مساحة القرص
> - يفعّل live-restore للحفاظ على تشغيل الحاويات أثناء تحديثات daemon
> - يزيد حدود واصفات الملفات للتطبيقات عالية الحركة

---

## 📱 الخطوة 3 — إنشاء تطبيقك

لنُنشئ تطبيق Node.js نموذجي ببنية احترافية.

### 3.1 إنشاء مجلد المشروع

```bash
mkdir power-of-4-app && cd power-of-4-app
```

### 3.2 إنشاء كود التطبيق

**server.js** — خادم HTTP بسيط لكن غني بالميزات:

```bash
cat > server.js <<'EOF'
const http = require('http');
const os = require('os');

const PORT = process.env.PORT || 3000;
const HOSTNAME = os.hostname();
const START_TIME = new Date();

const server = http.createServer((req, res) => {
  const uptime = Math.floor((Date.now() - START_TIME) / 1000);
  
  const response = {
    message: "🚀 مرحباً من قوة الأربعة!",
    timestamp: new Date().toISOString(),
    hostname: HOSTNAME,
    uptime_seconds: uptime,
    environment: process.env.NODE_ENV || 'development',
    endpoints: {
      health: '/health',
      info: '/info'
    }
  };

  // معالجة المسارات المختلفة
  if (req.url === '/health') {
    res.writeHead(200, { 'Content-Type': 'application/json' });
    res.end(JSON.stringify({ status: 'healthy', uptime_seconds: uptime }));
  } else if (req.url === '/info') {
    res.writeHead(200, { 'Content-Type': 'application/json' });
    res.end(JSON.stringify({
      node_version: process.version,
      platform: process.platform,
      memory: process.memoryUsage(),
      cpu: os.cpus()[0].model
    }));
  } else {
    res.writeHead(200, { 'Content-Type': 'application/json' });
    res.end(JSON.stringify(response, null, 2));
  }
});

server.listen(PORT, '0.0.0.0', () => {
  console.log(`✅ الخادم يعمل على http://0.0.0.0:${PORT}`);
  console.log(`📊 فحص الصحة: http://0.0.0.0:${PORT}/health`);
  console.log(`ℹ️  معلومات الخادم: http://0.0.0.0:${PORT}/info`);
});

// معالجة الإغلاق الرشيق
process.on('SIGTERM', () => {
  console.log('📴 تم استلام SIGTERM، جاري الإغلاق بشكل رشيق...');
  server.close(() => {
    console.log('👋 تم إغلاق الخادم');
    process.exit(0);
  });
});
EOF
```

### 3.3 إنشاء تكوين الحزمة

**package.json**:

```bash
cat > package.json <<'EOF'
{
  "name": "power-of-4-app",
  "version": "1.0.0",
  "description": "تطبيق تجريبي يعرض النشر مع Ubuntu + Docker + GitHub + Coolify",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "echo \"اجتازت الاختبارات!\" && exit 0"
  },
  "keywords": ["docker", "coolify", "deployment"],
  "author": "اسمك",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
EOF
```

### 3.4 إنشاء Dockerfile جاهز للإنتاج

**Dockerfile** — بناء متعدد المراحل لحجم صورة مثالي:

```bash
cat > Dockerfile <<'EOF'
# ============================================
# 🚀 قوة الأربعة - Dockerfile للإنتاج
# ============================================

# المرحلة 1: التبعيات
FROM node:20-alpine AS deps
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production && npm cache clean --force

# المرحلة 2: الإنتاج
FROM node:20-alpine AS production

# إضافة تسميات لتحسين الصيانة
LABEL maintainer="your-email@example.com"
LABEL version="1.0.0"
LABEL description="تطبيق قوة الأربعة التجريبي"

# إنشاء مستخدم غير root للأمان
RUN addgroup -g 1001 -S nodejs && \
    adduser -S nodejs -u 1001

WORKDIR /app

# نسخ التبعيات من مرحلة deps
COPY --from=deps /app/node_modules ./node_modules

# نسخ كود التطبيق
COPY --chown=nodejs:nodejs . .

# ضبط متغيرات البيئة
ENV NODE_ENV=production
ENV PORT=3000

# التبديل للمستخدم غير root
USER nodejs

# كشف المنفذ
EXPOSE 3000

# فحص الصحة
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
  CMD wget --no-verbose --tries=1 --spider http://localhost:3000/health || exit 1

# بدء التطبيق
CMD ["node", "server.js"]
EOF
```

### 3.5 إنشاء ملف Docker Ignore

**.dockerignore** — استبعاد الملفات غير الضرورية:

```bash
cat > .dockerignore <<'EOF'
node_modules
npm-debug.log
.git
.gitignore
*.md
.env.local
.env.*.local
Dockerfile*
docker-compose*
.dockerignore
EOF
```

### 3.6 إنشاء ملف Git Ignore

**.gitignore**:

```bash
cat > .gitignore <<'EOF'
node_modules/
.env
.env.local
*.log
.DS_Store
coverage/
dist/
EOF
```

### 3.7 إنشاء README

**README.md**:

```bash
cat > README.md <<'EOF'
# 🚀 تطبيق قوة الأربعة التجريبي

تطبيق توضيحي يعرض النشر مع **Ubuntu + Docker + GitHub + Coolify**.

## البدء السريع

### التطوير المحلي
```bash
npm install
npm run dev
```

### بناء Docker
```bash
docker build -t power-of-4-app .
docker run -p 3000:3000 power-of-4-app
```

## نقاط النهاية

| النقطة | الوصف |
|--------|-------|
| `/` | رسالة ترحيب مع معلومات التطبيق |
| `/health` | فحص الصحة للمراقبة |
| `/info` | معلومات النظام |

## النشر

يُنشر هذا التطبيق تلقائياً عبر **Coolify** عند دفع التغييرات لفرع `main`.

## الرخصة

MIT
EOF
```

### 3.8 تهيئة Git والدفع إلى GitHub

```bash
# تهيئة مستودع git
git init

# إضافة جميع الملفات
git add .

# إنشاء commit أولي
git commit -m "🚀 Commit أولي: تطبيق قوة الأربعة التجريبي"

# تعيين main كفرع افتراضي
git branch -M main

# إضافة مستودع GitHub كـ remote (استبدل بعنوان مستودعك)
git remote add origin git@github.com:<اسم-المستخدم>/<اسم-المستودع>.git

# الدفع إلى GitHub
git push -u origin main
```

> 💡 **نصيحة**: أنشئ مستودع GitHub أولاً على https://github.com/new، ثم نفّذ الأوامر أعلاه.

---

## ☁️ الخطوة 4 — تثبيت Coolify

Coolify هو السحر الذي يربط كل شيء معاً — بديل مستضاف ذاتياً لـ Heroku.

### 4.1 تشغيل سكربت التثبيت

```bash
curl -fsSL https://cdn.coollabs.io/coolify/install.sh | bash
```

سيقوم هذا السكربت بـ:
- ✅ تثبيت Docker (إن لم يكن موجوداً)
- ✅ إعداد Docker Compose
- ✅ نشر حاويات Coolify
- ✅ تكوين الشبكات
- ✅ إعداد المجلدات الدائمة

### 4.2 الإعداد الأولي

1. **الوصول للوحة تحكم Coolify**:
   افتح المتصفح وانتقل إلى:
   ```
   http://<عنوان-ip-الخادم>:8000
   ```

2. **إنشاء حساب المشرف**:
   - أدخل عنوان بريدك الإلكتروني
   - عيّن كلمة مرور قوية
   - أكمل التسجيل

3. **إكمال الإعداد**:
   - اتبع معالج الإعداد
   - كوّن إعدادات الخادم
   - أعدّ إشعارات البريد الإلكتروني (اختياري)

> 🎉 **تهانينا!** Coolify يعمل الآن على خادمك.

---

## 🌐 الخطوة 5 — تكوين نطاقك

### 5.1 إعداد سجلات DNS

1. سجّل الدخول لموفر DNS الخاص بك (Cloudflare، Route53، إلخ.)
2. أنشئ السجلات التالية:

| النوع | الاسم | القيمة | TTL |
|-------|-------|--------|-----|
| A | @ (أو نطاق فرعي) | `<عنوان-ip-الخادم>` | 300 |
| A | coolify | `<عنوان-ip-الخادم>` | 300 |
| AAAA | @ (اختياري) | `<عنوان-ipv6>` | 300 |

### 5.2 إضافة النطاق في Coolify

1. في Coolify: **Settings → Server → Domains**
2. أضف نطاقك: `app.yourdomain.com`
3. أضف نطاق Coolify: `coolify.yourdomain.com`
4. احفظ التغييرات

### 5.3 التحقق من انتشار DNS

```bash
# التحقق من حل DNS
dig +short app.yourdomain.com
nslookup app.yourdomain.com

# اختبار الاتصال
curl -I http://app.yourdomain.com
```

---

## 🔗 الخطوة 6 — ربط GitHub مع Coolify

### 6.1 إنشاء رمز وصول GitHub الشخصي

1. اذهب إلى https://github.com/settings/tokens/new
2. كوّن الرمز:
   - **ملاحظة**: `Coolify Deployment`
   - **انتهاء الصلاحية**: 90 يوماً (أو مخصص)
   - **الصلاحيات**:
     - ✅ `repo` (تحكم كامل بالمستودعات الخاصة)
     - ✅ `admin:repo_hook` (تحكم كامل بـ webhooks المستودعات)
3. انقر **Generate token**
4. **انسخ الرمز فوراً** (لن تراه مرة أخرى!)

### 6.2 إضافة GitHub في Coolify

1. في Coolify: **Settings → Git Providers**
2. انقر **Add GitHub**
3. الصق رمز الوصول الشخصي
4. انقر **Save**
5. انتظر التحقق ✅

> 🔒 **ملاحظة أمنية**: احفظ PAT بأمان. إذا تم اختراقه، ألغِه فوراً على https://github.com/settings/tokens

---

## 🚀 الخطوة 7 — نشر تطبيقك

هنا يحدث السحر!

### 7.1 إنشاء مشروع جديد

1. في Coolify: **Projects → New Project**
2. الاسم: `قوة الأربعة`
3. الوصف: `مشروع نشر تجريبي`

### 7.2 إضافة تطبيقك

1. انقر **New Resource → Application**
2. اختر **Git Repository**
3. اختر حساب **GitHub** المتصل
4. اختر مستودعك وفرع `main`

### 7.3 تكوين إعدادات البناء

| الإعداد | القيمة |
|---------|--------|
| **Build Pack** | Dockerfile |
| **مسار Dockerfile** | `Dockerfile` |
| **المنفذ** | `3000` |
| **النشر التلقائي** | ✅ مفعّل |

### 7.4 ضبط متغيرات البيئة (اختياري)

أضف أي متغيرات بيئة يحتاجها تطبيقك:

```
NODE_ENV=production
```

### 7.5 انشر!

1. انقر **Deploy** 🚀
2. راقب سجلات البناء في الوقت الفعلي
3. انتظر اكتمال النشر

> ⏱️ **النشر الأول** يستغرق عادة 2-5 دقائق بينما يسحب Docker الصور الأساسية.

---

## 🔐 الخطوة 8 — تفعيل HTTPS مع Let's Encrypt

### 8.1 تكوين النطاق لتطبيقك

1. اذهب لإعدادات تطبيقك
2. انتقل لقسم **Domains**
3. أضف: `app.yourdomain.com`
4. فعّل **Enable SSL** ✅

### 8.2 التحقق من HTTPS

```bash
# التحقق من الشهادة
curl -vI https://app.yourdomain.com 2>&1 | grep -A 10 "Server certificate"

# اختبار التطبيق
curl https://app.yourdomain.com
```

الاستجابة المتوقعة:
```json
{
  "message": "🚀 مرحباً من قوة الأربعة!",
  "timestamp": "2025-12-19T20:00:00.000Z",
  "hostname": "container-id",
  "uptime_seconds": 120,
  "environment": "production"
}
```

---

## ✅ الخطوة 9 — التحقق والمراقبة

### 9.1 سجلات التطبيق

الوصول للسجلات الفورية في Coolify:
**Application → Logs**

أو عبر SSH:
```bash
docker logs -f $(docker ps -q --filter "name=power-of-4")
```

### 9.2 فحوصات الصحة

```bash
# فحص صحة سريع
curl -s https://app.yourdomain.com/health | jq
# {"status":"healthy","uptime_seconds":3600}

# رؤوس HTTP الكاملة
curl -I https://app.yourdomain.com

# مراقبة مستمرة
watch -n 5 'curl -s https://app.yourdomain.com/health'
```

### 9.3 سير عمل النشر

لنشر التحديثات:

1. أجرِ تغييرات على الكود محلياً
2. قم بـ commit والدفع:
   ```bash
   git add .
   git commit -m "✨ إضافة ميزة جديدة"
   git push origin main
   ```
3. Coolify تلقائياً:
   - يستقبل webhook من GitHub
   - يسحب أحدث كود
   - يبني صورة Docker جديدة
   - ينشر بدون توقف

---

## 🛠️ الصيانة واستكشاف الأخطاء

### المشاكل الشائعة والحلول

#### 🔴 Docker Daemon لا يستجيب

```bash
# إعادة تشغيل Docker
sudo systemctl restart docker

# التحقق من الحالة
sudo systemctl status docker

# عرض سجلات Docker
sudo journalctl -u docker -f
```

#### 🔴 فشل البناء

1. تحقق من صيغة Dockerfile
2. تأكد من وجود جميع الملفات المطلوبة في المستودع
3. راجع سجلات بناء Coolify للأخطاء المحددة
4. اختبر بناء Docker محلياً:
   ```bash
   docker build -t test-build .
   ```

#### 🔴 مشاكل شهادة SSL/TLS

```bash
# التحقق من فتح المنافذ
sudo ufw status
sudo netstat -tlnp | grep -E '80|443'

# التحقق من DNS
dig +short yourdomain.com

# تحقق من حدود Let's Encrypt
# https://letsencrypt.org/docs/rate-limits/
```

#### 🔴 فشل GitHub Webhook

1. في Coolify: **Settings → Git Providers → Webhook**
2. انسخ عنوان webhook
3. في GitHub: **Settings → Webhooks**
4. تحقق من تكوين webhook بشكل صحيح
5. راجع التسليمات الأخيرة للأخطاء

### أوامر مفيدة

```bash
# سرد جميع الحاويات
docker ps -a

# استخدام قرص Docker
docker system df

# تنظيف الموارد غير المستخدمة
docker system prune -a

# عرض حاويات Coolify
docker ps --filter "name=coolify"

# إعادة تشغيل Coolify
docker restart coolify
```

---

## 🎨 التخصيصات المتقدمة

### نشر متعدد البيئات

إعداد بيئات staging وproduction:

```yaml
# docker-compose.staging.yml
version: '3.8'
services:
  app:
    build: .
    environment:
      - NODE_ENV=staging
      - API_URL=https://api-staging.example.com
```

### إضافة قاعدة بيانات

في Coolify، أضف قاعدة بيانات PostgreSQL:

1. **New Resource → Database → PostgreSQL**
2. كوّن بيانات الاعتماد
3. انسخ سلسلة الاتصال
4. أضف لمتغيرات بيئة تطبيقك:
   ```
   DATABASE_URL=postgresql://user:pass@host:5432/db
   ```

### بريد نطاق مخصص

تكوين إرسال البريد عبر SMTP:
1. **Settings → Notification Channels**
2. أضف تكوين SMTP
3. اختبر مع إشعارات النشر

---

## 📊 المراقبة والسجلات

### إعداد Prometheus + Grafana

```bash
# إنشاء docker-compose.monitoring.yml
cat > docker-compose.monitoring.yml <<'EOF'
version: '3.8'
services:
  prometheus:
    image: prom/prometheus:latest
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml

  grafana:
    image: grafana/grafana:latest
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
EOF

docker compose -f docker-compose.monitoring.yml up -d
```

### تجميع السجلات مع Loki

في Coolify، فعّل تكامل Loki للسجلات المركزية.

---

## ❓ الأسئلة الشائعة

### س: هل يمكنني نشر تطبيقات متعددة على نفس الخادم؟

**ج:** بالتأكيد! Coolify يتفوق في تشغيل تطبيقات متعددة. كل تطبيق يحصل على:
- حاويته الخاصة
- نطاق/نطاق فرعي فريد
- بيئة معزولة
- خط نشر مستقل

### س: كيف أستعيد إصداراً سابقاً؟

**ج:** في Coolify:
1. اذهب لتبويب **Deployments**
2. ابحث عن النشر الناجح السابق
3. انقر **Rollback**

### س: ما متطلبات الخادم؟

**ج:** المتطلبات الدنيا:
| المورد | الأدنى | الموصى به |
|--------|--------|-----------|
| CPU | 1 vCPU | 2+ vCPU |
| RAM | 2 GB | 4+ GB |
| التخزين | 20 GB | 40+ GB SSD |

### س: هل يمكنني استخدام GitLab بدلاً من GitHub؟

**ج:** نعم! Coolify يدعم:
- GitHub
- GitLab  
- Bitbucket
- Gitea
- أي مستودع Git عبر URL

### س: كيف أحدّث Coolify؟

**ج:** 
```bash
curl -fsSL https://cdn.coollabs.io/coolify/install.sh | bash
```

---

## 🎯 تكامل CI/CD

أضف اختباراً آلياً قبل النشر مع GitHub Actions:

**.github/workflows/ci.yml**:

```yaml
name: 🧪 خط أنابيب CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    name: 🔍 اختبار وفحص
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 جلب الكود
        uses: actions/checkout@v4

      - name: 📦 إعداد Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📚 تثبيت التبعيات
        run: npm ci

      - name: 🧪 تشغيل الاختبارات
        run: npm test

      - name: 🐳 بناء صورة Docker
        run: docker build -t test-image .

      - name: ✅ تم بناء الصورة بنجاح
        run: echo "صورة Docker جاهزة للنشر!"

  security:
    name: 🔒 فحص الأمان
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 جلب الكود
        uses: actions/checkout@v4

      - name: 🔍 تشغيل ماسح ثغرات Trivy
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
```

---

## 🎉 الخاتمة

تهانينا! 🎊 لقد بنيت بنجاح خط نشر حديث باستخدام **قوة الأربعة**:

| التقنية | الدور | الإنجاز |
|---------|-------|---------|
| **Ubuntu** | الأساس | خادم آمن ومستقر |
| **Docker** | الحاويات | بناء قابل للتكرار |
| **GitHub** | التحكم بالمصدر | إدارة الإصدارات ومحفزات الأتمتة |
| **Coolify** | التنسيق | نشر بنقرة واحدة مع SSL |

### ما حققته

✅ إعداد خادم Ubuntu آمن مع Docker  
✅ إنشاء تطبيق جاهز للإنتاج في حاوية  
✅ تكوين نشر تلقائي من GitHub  
✅ تفعيل HTTPS بشهادات SSL مجانية  
✅ إنشاء مراقبة وسجلات  

### الخطوات التالية

1. **🗄️ إضافة قاعدة بيانات**: PostgreSQL أو MySQL أو Redis عبر Coolify
2. **🔀 بيئات متعددة**: إعدادات Staging + Production
3. **📈 مراقبة متقدمة**: إعداد Prometheus + Grafana
4. **🔐 تعزيز الأمان**: تنفيذ fail2ban، مفاتيح SSH فقط
5. **⚖️ التوسع**: إضافة موازنة الحمل للحركة العالية

### الموارد

- 📖 [توثيق Coolify](https://coolify.io/docs)
- 🐙 [توثيق GitHub Actions](https://docs.github.com/en/actions)
- 🐳 [أفضل ممارسات Docker](https://docs.docker.com/develop/develop-images/dockerfile_best-practices/)
- 🖥️ [دليل خادم Ubuntu](https://ubuntu.com/server/docs)

---

> 💬 **لديك أسئلة؟** افتح issue على GitHub أو تواصل مع مجتمع Coolify!

---

##### License: MIT

<!--

Contributor's Certificate of Origin

By making a contribution to this project, I certify that:

(a) The contribution was created in whole or in part by me and I have
    the right to submit it under the license indicated in the file; or

(b) The contribution is based upon previous work that, to the best of my
    knowledge, is covered under an appropriate license and I have the
    right under that license to submit that work with modifications,
    whether created in whole or in part by me, under the same license
    (unless I am permitted to submit under a different license), as
    indicated in the file; or

(c) The contribution was provided directly to me by some other person
    who certified (a), (b) or (c) and I have not modified it.

(d) I understand and agree that this project and the contribution are
    public and that a record of the contribution (including all personal
    information I submit with it, including my sign-off) is maintained
    indefinitely and may be redistributed consistent with this project
    or the license(s) involved.

Signed-off-by: [submitter's name and email address here]

-->
